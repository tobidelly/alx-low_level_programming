#include "main.h"

/**
 * _islower - checks if alphabet is lowercase
 * @c: is the character or alphabet
 * Return: 1
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	return (1);
		else
		return (0);
}
