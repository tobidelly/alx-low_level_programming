#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0 (Success)
 *
 */

int main(int argc, char *argv[])
{
	(void)argc; /* avoid unused parameter */
	printf("%s\n", *argv);

	return (0);
}
